import { KontrahentToStringPipe } from './kontrahent-to-string.pipe';

describe('KontrahentToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new KontrahentToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
